{{- if .Values.pump.service -}}
apiVersion: v1
kind: Service
metadata:
  name: pump-svc-{{ include "tyk-pro.fullname" . }}
  labels:
    app: pump-svc-{{ include "tyk-pro.fullname" . }}
    chart: {{ include "tyk-pro.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
  - port: {{ .Values.pump.service.port }}
    targetPort: {{ .Values.pump.containerPort }}
    protocol: TCP
  type: {{ .Values.pump.service.type }}
  selector:
    app: pump-{{ include "tyk-pro.fullname" . }}
    release: {{ .Release.Name }}
{{- end}} 
