{{- if or .Values.gateway.enabled .Values.gateway.certificates }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mgmt-gateway-conf-{{ include "tyk-pro.fullname" . }}
  labels:
    app: gateway-{{ include "tyk-pro.fullname" . }}
    chart: {{ include "tyk-pro.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  tyk_mgmt.conf: |
    {
      "http_server_options": {
        "certificates": [
        {{- range $i, $certset := .Values.gateway.certificates }}
          {
            "cert_file": "/etc/certs/{{ $certset.certFilename }}",
            "key_file": "/etc/certs/{{ $certset.keyFilename }}"
          }{{- if lt $i (len .Values.gateway.certificates) }},{{- end }}
        {{- end }}
        ]
      }
    }  
{{- end }}
