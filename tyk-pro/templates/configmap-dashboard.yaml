{{- if or .Values.dash.enabled .Values.dash.certificates }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-conf-{{ include "tyk-pro.fullname" . }}
  labels:
    app: dashboard-{{ include "tyk-pro.fullname" . }}
    chart: {{ include "tyk-pro.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  tyk_analytics.conf: |
    {
      "http_server_options": {
        "certificates": [
        {{- range $i, $certset := .Values.dash.certificates }}
          {
            "cert_file": "/etc/certs/{{ $certset.certFilename }}",
            "key_file": "/etc/certs/{{ $certset.keyFilename }}"
          }{{- if lt $i (len .Values.dash.certificates) }},{{- end }}
        {{- end }}
        ]
      }
    }  
{{- end }}
