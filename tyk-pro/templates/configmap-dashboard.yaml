{{- if .Values.dash.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-conf-{{ include "tyk-pro.fullname" . }}
  labels:
    app: dashboard-{{ include "tyk-pro.fullname" . }}
    chart: {{ include "tyk-pro.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  tyk_analytics.conf: |
    {
      "http_server_options": {
        "certificates": [
        {{- $length := len .Values.dash.certificates -}}
        {{- range $i, $certset := .Values.dash.certificates }}
          {
            "cert_file": "/etc/certs/crts/{{ $certset.certFilename }}",
            "key_file": "/etc/certs/keys/{{ $certset.keyFilename }}"
          }{{- if lt $i (sub $length 1) }},{{- end }}
        {{- end }}
        ]
      }
    }  
{{- end }}
