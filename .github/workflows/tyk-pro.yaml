name: Tyk Pro Push Helm
on: push
defaults:
  run:
    working-directory: ./tyk-pro
jobs:
  push:
    name: Tyk Pro Push CS
    runs-on: ubuntu-latest
    branches:
      - main
      - release-**
    paths:
      - 'tyk-pro/**'
    steps:
      - uses: actions/checkout@v2
      - name: Push
        id: push
        uses: cloudsmith-io/action@master
        with:
          api-key: ${{ secrets.CLOUDSMITH_API_KEY }}
          command: 'push'
          format: 'helm'
          owner: 'tyk'
          repo: 'tyk-pro'
          version: ${{ github.sha }}
          file: 'test/fixture/tyk-pro-helm-chart-v0.0.1.tar.gz'