{{- if and .Values.tyk-pro.enabled .Values.tyk-pro.dash.enabled .Values.tyk-pro.dash.ingress.enabled -}}
{{- $fullName := include "tyk-chart.fullname" . -}}
{{- $ingressPath := .Values.tyk-pro.dash.ingress.path -}}
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: dash-ing-{{ $fullName }}
  labels:
    app: dash-ing-{{ $fullName }}
    chart: {{ include "tyk-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- if .Values.tyk-pro.dash.ingress.annotations }}
  annotations:
  {{- range $key, $value := .Values.tyk-pro.dash.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
spec:
{{- if .Values.tyk-pro.dash.ingress.tls }}
  tls:
  {{- range .Values.tyk-pro.dash.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
  {{- range .Values.tyk-pro.dash.ingress.hosts }}
    - host: {{ . | quote }}
      http:
        paths:
          - path: {{ $ingressPath }}
            backend:
              serviceName: dashboard-svc-{{ $fullName }}
              servicePort: http
  {{- end }}
{{- end }}