{{- if and .Values.tyk-pro.enabled .Values.pump.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: pump-conf-{{ include "tyk-chart.fullname" . }}
  labels:
    app: pump-{{ include "tyk-chart.fullname" . }}
    chart: {{ include "tyk-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  {{- (.Files.Glob "configs/pump.conf").AsConfig | nindent 2 }}
{{- end }}
