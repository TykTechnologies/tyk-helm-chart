{{- if and .Values.tyk-pro.enabled .Values.dash.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-conf-{{ include "tyk-chart.fullname" . }}
  labels:
    app: dashboard-{{ include "tyk-chart.fullname" . }}
    chart: {{ include "tyk-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  {{- (.Files.Glob "configs/tyk_analytics.conf").AsConfig | nindent 2 }}
{{- end }}
