apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-login-details
  labels:
    app: login-details-{{ include "dashboard.fullname" . }}
    chart: {{ include "dashboard.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  {{ if .Values.bootstrap.adminUser.password }}
  TYK_PASS: {{ .Values.bootstrap.adminUser.password | b64enc | quote }}
  {{ else }}
  TYK_PASS: {{ randAlphaNum 10 | b64enc | quote }}
  {{ end }}